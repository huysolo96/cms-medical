import { cellComponents } from './table-cells';
import { dialogForms } from './dialog-forms';
import { dialogs } from './dialogs';

export * from './table-cells';

export const components = [
    cellComponents,
    dialogForms,
    dialogs
];

export const entryComponents = [
    dialogForms,
    dialogs
];
